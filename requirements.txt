# ═══════════════════════════════════════════════════════
#  Dorm-Net requirements.txt
#  Tested on Python 3.10 / 3.11 · Windows 10/11 · Ubuntu 22.04
# ═══════════════════════════════════════════════════════

# ── UI ──────────────────────────────────────────────────
streamlit>=1.35.0,<2.0.0

# ── LLM / Ollama client ─────────────────────────────────
httpx>=0.27.0               # async-capable HTTP for Ollama streaming
ollama>=0.2.0               # optional: official Ollama Python SDK (fallback)

# ── Embeddings (CPU-optimised, ~80 MB) ──────────────────
sentence-transformers>=3.0.0
# pins a safe torch CPU build — remove torch line if you have a GPU
torch>=2.1.0               # sentence-transformers dependency

# ── Vector DB ────────────────────────────────────────────
chromadb>=0.5.0
pysqlite3-binary>=0.5.2    # ships its own sqlite3 >= 3.35 (needed by chromadb
                            # on systems with older system sqlite — e.g. Ubuntu 20)

# ── PDF ingestion ────────────────────────────────────────
PyMuPDF>=1.24.0             # fitz — fast, no Java dependency

# ── Vision / OCR ─────────────────────────────────────────
opencv-python-headless>=4.9.0   # headless: no GUI deps (smaller, CI-safe)
pytesseract>=0.3.10
Pillow>=10.0.0
numpy>=1.24.0

# ── Utilities ────────────────────────────────────────────
python-dotenv>=1.0.0        # optional: load TESSERACT_CMD etc. from .env
